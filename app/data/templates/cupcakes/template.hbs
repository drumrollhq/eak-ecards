<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="//fonts.googleapis.com/css?family=Playball" rel="stylesheet" type="text/css">
    <style>
      html, body {
        min-width: 100%;
        min-height: 100%;
      }

      body {
        font-family: 'Playball', cursive;
        font-size: 26px;
        margin: 0;
        padding: 0;
        background-color: #F1D7FF;
        background-image: -webkit-linear-gradient(top, #F8F9DB, #DDD9FC);
        background-image:    -moz-linear-gradient(top, #F8F9DB, #DDD9FC);
        background-image:         linear-gradient(top, #F8F9DB, #DDD9FC);
      }

      h1 {
        font-weight: normal;
      }

      .card {
        max-width: 600px;
        margin: auto;
        padding: 30px;
      }

      p {
        text-align: right;
      }

      .to {
        text-align: left;
      }

      h1 {
        text-align: center;
      }

      img {
        display: block;
        width: 100%;
        max-width: 400px;
        margin: auto;
      }
    </style>
  </head>
  <body>
    <section class="card">
      <p class="to">{{to}}</p>
      <h1>{{message}}</h1>
      <img src="{{img}}" alt="cup cups!">
      <p>{{message2}}</p>
      <p>{{from}}</p>
    </section>
  </body>
</html>
