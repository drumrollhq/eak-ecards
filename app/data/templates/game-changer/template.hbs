<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="//fonts.googleapis.com/css?family=Josefin+Slab" rel="stylesheet" type="text/css">
    <style>
      html, body {
        min-width: 100%;
        min-height: 100%;
      }

      body {
        font-family: 'Josefin Slab', cursive;
        font-size: 22px;
        margin: 0;
        padding: 0;
        background-color: #F1D7FF;
        background-image: -webkit-linear-gradient(top, #FEE6D6, #FCF3C5, #EBFDFF);
        background-image:    -moz-linear-gradient(top, #FEE6D6, #FCF3C5, #EBFDFF);
        background-image:         linear-gradient(top, #FEE6D6, #FCF3C5, #EBFDFF);
      }

      .card {
        max-width: 600px;
        margin: auto;
        padding: 30px;
      }

      h1, h2 {
        text-align: center;
      }

      h1 {
        font-weight: normal;
        margin-bottom: 0;
      }

      h2 {
        margin-top: 15px;
      }

      img {
        display: block;
        width: 100%;
        max-width: 350px;
        margin: 50px auto;
      }

      p {
        text-align: center;
      }
    </style>
  </head>
  <body>
    <section class="card">
      <h1>{{message1}}</h1>
      <h2>{{message2}}</h2>
      <img src="{{img}}" alt="Peaches Jerome and Bob, Squirrel of the South Seas">
      <p>{{message3}}</p>
      <p class="from">{{from}}</p>
    </section>
  </body>
</html>
