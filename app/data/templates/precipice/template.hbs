<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="//fonts.googleapis.com/css?family=Tangerine" rel="stylesheet" type="text/css">
    <style>
      html, body {
        min-width: 100%;
        min-height: 100%;
      }

      body {
        font-family: 'Tangerine', cursive;
        font-size: 32px;
        margin: 0;
        padding: 0;
        background-image: -webkit-linear-gradient(top, #FCE0FB, #FEBDFF);
        background-image:    -moz-linear-gradient(top, #FCE0FB, #FEBDFF);
        background-image:         linear-gradient(top, #FCE0FB, #FEBDFF);
      }

      .card {
        max-width: 600px;
        margin: auto;
        padding: 30px;
      }

      img {
        display: block;
        width: 100%;
        max-width: 170px;
        margin: auto;
        margin-top: 50px;
      }

      h1 {
        font-weight: normal;
        text-align: center;
      }

      p {
        text-align: right;
        margin: 10px 0;
      }

      .to {
        text-align: left;
      }
    </style>
  </head>
  <body>
    <section class="card">
      <p class="to">{{to}}</p>
      <h1>{{message}}</h1>
      <p>{{message2}}</p>
      <img src="{{img}}" alt="Johnny McGrew">
    </section>
  </body>
</html>
