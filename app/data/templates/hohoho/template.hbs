<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="//fonts.googleapis.com/css?family=Tangerine" rel="stylesheet" type="text/css">
    <style type="text/css">
      @font-face {
        font-family: "KgNextToMe";
        src: url('/fonts/KGNextToMe-Solid.ttf') format("truetype");
      }

      html, body {
        min-width: 100%;
        min-height: 100%;
      }

      body {
        font-family: 'Tangerine', cursive;
        font-size: 26px;
        margin: 0;
        padding: 0;
        background-color: #dcf7f6;
        color: white;
      }

      section {
        max-width: 540px;
        margin: auto;
        padding: 0 30px;
      }

      .card{
        background-image: url({{img}});
        background-size: cover;
        height: 837px;
        position: relative;
      }

      img {
        display: block;
        width: 100%;
        max-width: 400px;
        margin: auto;
        margin-top: 50px;
      }

      h1 {
        font-family:'KgNextToMe';
        font-weight: normal;
        text-align: center;
        font-size: 2.5em;
        margin: 0;
        padding-top: 30px;
      }

      p {
        text-align: right;
        margin: 10px 0;
      }

      .greeting{
        color: black;
        position: absolute;
        bottom: 10px;
        right: 25px;
        font-size: 1.2em;
      }
    </style>
  </head>
  <body>
    <section class="card">
      <h1>{{message}}</h1>
      <div class="greeting">
        <p>{{to}}</p>
        <p>{{from}}</p>
      </div>
    </section>
  </body>
</html>
